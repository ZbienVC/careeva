[phases.install]
# --include=dev is required: Railway builds with NODE_ENV=production, which
# makes npm skip devDependencies — but `next build` needs typescript/tailwind.
cmds = ["npm install --legacy-peer-deps --include=dev"]
# cache-bust: 2026-06-10-storage
